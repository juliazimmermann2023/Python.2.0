import math

def circulo(raio):
    area = math.pi * raio ** 2
    return area

def quadrado(lado):
    area = lado ** 2
    return area

def triangulo(base, altura):
    area = (base * altura) / 2
    return area

raio = 5
lado_quadrado = 4
base_triangulo = 6
altura_triangulo = 3

area_circulo = circulo(raio)
area_quadrado = quadrado(lado_quadrado)
area_triangulo = triangulo(base_triangulo, altura_triangulo)

print(f'Área do círculo com raio {raio}: {area_circulo:.2f}')
print(f'Área do quadrado com lado {lado_quadrado}: {area_quadrado}')
print(f'Área do triângulo com base {base_triangulo} e altura {altura_triangulo}: {area_triangulo}')
